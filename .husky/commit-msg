#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Solo validar el mensaje del commit, no modificar archivos
# Los archivos ya fueron formateados y linted en pre-commit
npx --no -- commitlint --edit 
